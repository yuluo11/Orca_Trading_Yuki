"""Cross-domain reusable skills.

These skills should remain domain-agnostic and focus on generic backend
capabilities that can be shared across analyst, decision, and reflection flows.

Suggested use:
- ``context``: context assembly and reduction helpers
- ``normalization``: stable text / payload normalization helpers
- ``routing``: lightweight task routing or capability selection helpers
"""
